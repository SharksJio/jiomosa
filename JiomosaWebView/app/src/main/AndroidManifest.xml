<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Optional: Write external storage for downloads (API < 29) -->
    <uses-permission 
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JiomosaWebView"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Main Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JiomosaWebView.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <!-- Outlook Mail Shortcut -->
        <activity
            android:name=".OutlookActivity"
            android:exported="true"
            android:label="@string/app_name_outlook"
            android:icon="@drawable/ic_jiomail"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:taskAffinity="com.jiomosa.webview.outlook">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="default_url"
                android:value="https://outlook.office.com/mail" />
        </activity>

        <!-- Microsoft Teams Shortcut -->
        <activity
            android:name=".TeamsActivity"
            android:exported="true"
            android:label="@string/app_name_teams"
            android:icon="@drawable/ic_jioteams"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:taskAffinity="com.jiomosa.webview.teams">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="default_url"
                android:value="https://teams.microsoft.com" />
        </activity>

        <!-- OneDrive Shortcut -->
        <activity
            android:name=".OneDriveActivity"
            android:exported="true"
            android:label="@string/app_name_onedrive"
            android:icon="@drawable/ic_jiodrive"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:taskAffinity="com.jiomosa.webview.onedrive">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="default_url"
                android:value="https://rilcloud-my.sharepoint.com" />
        </activity>

        <!-- Whiteboard Shortcut -->
        <activity
            android:name=".WhiteboardActivity"
            android:exported="true"
            android:label="@string/app_name_whiteboard"
            android:icon="@drawable/ic_jiowhiteboard"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"
            android:taskAffinity="com.jiomosa.webview.whiteboard">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="default_url"
                android:value="https://whiteboard.cloud.microsoft/" />
        </activity>

    </application>

</manifest>
