<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default configuration for all domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Domain-specific configurations -->
    
    <!-- Allow cleartext for local development servers only -->
    <!-- IMPORTANT: Remove or restrict these entries in production -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator host -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Localhost -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- Jiomosa server configuration (adjust for your deployment) -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Replace with your actual Jiomosa server domain -->
        <domain includeSubdomains="true">jiomosa.local</domain>
    </domain-config>

</network-security-config>
