# FastAPI and async support
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# WebRTC support
aiortc==1.7.0
av==11.0.0

# Browser automation (Playwright - lighter than Selenium)
playwright==1.41.0

# WebSocket for signaling
websockets==12.0

# Redis for session management (optional)
redis==5.0.1
aioredis==2.0.1

# Utilities
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Monitoring and logging
prometheus-client==0.19.0

# CORS support
fastapi-cors==0.0.6

# JWT for authentication
pyjwt==2.8.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Pillow for image processing
Pillow==10.1.0

# Numpy for efficient image processing
numpy==1.26.3

# For CDP base64 decoding
# (base64 is built-in, no package needed)


