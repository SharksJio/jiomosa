<?xml version="1.0" encoding="utf-8"?>
<!--
    network_security_config.xml - Network security configuration
    
    This file should be placed in res/xml/network_security_config.xml
    
    Required for:
    - Allowing cleartext (HTTP) traffic for development
    - Custom certificate handling (if needed)
    
    For production, remove or restrict cleartext domains.
-->
<network-security-config>
    <!-- Default configuration for all domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Domain-specific configurations -->
    
    <!-- Allow cleartext for local development servers only -->
    <!-- IMPORTANT: Remove or restrict these entries in production -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator host -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Localhost -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- Jiomosa server configuration (adjust for your deployment) -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Replace with your actual Jiomosa server domain -->
        <domain includeSubdomains="true">jiomosa.local</domain>
    </domain-config>

    <!-- Debug configuration (remove in production) -->
    <!--
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    -->

</network-security-config>
