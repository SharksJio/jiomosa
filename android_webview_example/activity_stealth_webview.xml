<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_stealth_webview.xml - Layout for Stealth WebView Activity
    
    Simple layout containing:
    - WebView for rendering web content
    - ProgressBar for loading indication
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context=".StealthWebViewActivity">

    <!-- Main WebView -->
    <WebView
        android:id="@+id/stealthWebView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none" />

    <!-- Progress Bar (shown during page load) -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_alignParentTop="true"
        android:indeterminate="false"
        android:max="100"
        android:progress="0"
        android:progressDrawable="@drawable/progress_bar_gradient"
        android:visibility="gone" />

</RelativeLayout>
